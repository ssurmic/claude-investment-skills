# Skills NOT part of this investment toolkit — kept locally for the repo
# owner's personal workflows, never published. New entries go here, not in
# commit history. If one of these slipped into git tracking, untrack with:
#   git rm -r --cached <dir>
agent-reach
cr-review
create-amazon-internal-sims
design-doc
humanize-tone
playwright-cli
publish-cr
rca
research
ticket-ops

# Local settings (may contain machine-specific paths)
.claude/

# OS / editor cruft
.DS_Store
.vscode/
.idea/
*.swp

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.pytest_cache/
.venv/
venv/
*.egg-info/

# Logs / temp
*.log
*.tmp

# Secrets — NEVER commit these
.env
.env.local
.env.*.local
*.pem
*.key
secrets.json
credentials.json
# (price-alert/alerts.json is OK to commit — has no secrets, just alert config)
# (.env.example is OK to commit — template with no real values)
